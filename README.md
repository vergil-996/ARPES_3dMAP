# ARPES-3d-MAP

三维数据可视化与分析工具，面向本地桌面实验环境，提供 `npz` / `mat` 数据加载、3D 体渲染、2D 切片、去噪、积分分析、DOS 分析、截图与结果导出等能力。

右侧控件栏界面由三部分组成：

- 图像控制：负责加载数据、时间轴滑动、切片范围、交互切片和基础截图导出
- 渲染控制：负责色图、黑白场/Gamma、强度映射和去噪设置
- 处理分析：负责时间积分、坐标轴积分、切片内强度积分、能级态密度曲线、EDC曲线、EDC瀑布图、对2d图像进行二阶导，以及结果页导出


## 参与贡献
    本项目所用GUI，基于github上ChinaIceF大佬的开源项目修改而来，参考大佬github的源码地址：https://github.com/ChinaIceF/PyQt-SiliconUI

## 界面预览

> 以下示例基于作者本机静态谱样例 `NiHITP_calibrated_2.npz` 拍摄。由于该样例在 bin 数据时方向装反，截图前已开启“坐标反转”。该样例的 `sample` 形状为 `(500, 200, 200)`，属于静态 3D 数据，因此这里只展示其能覆盖的功能；时间积分和切片内强度积分在 README 中保留文字说明，不展示无效截图。

**3D 体渲染视图**

![3D 体渲染](docs/images/main-3d-plot.png)

**E 切片视图**

![E 切片](docs/images/slice-2d.png)

**坐标轴积分结果页**

![坐标轴积分](docs/images/axis-integral.png)

**能级态密度结果页**

![Energy DOS](docs/images/energy-dos.png)

## 安装与运行

Windows PowerShell:

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
python start.py
```

如果 PowerShell 提示 `Activate.ps1` 被禁用，可以直接使用虚拟环境里的 Python 运行：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe start.py
```

macOS / Linux:

```bash
python3.12 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
python start.py
```

说明：

- 依赖列表见 [requirements.txt](requirements.txt)
- 启动入口为 [start.py](start.py)
- `requirements.txt` 中包含 `PyQt-SiliconUI` 的 GitHub 依赖，首次安装需要联网

如果需要打包为桌面程序，可直接使用仓库中的 PyInstaller 配置：

```powershell
pyinstaller ARPES_3dMAP.spec
```

配置文件见 [ARPES_3dMAP.spec](ARPES_3dMAP.spec)。

## 支持的数据格式

### 推荐的 `npz` 字段

推荐输入字段如下：

```text
sample : 3D (Kx, Ky, E) 或 4D (Kx, Ky, E, T)
kx     : Kx 坐标，一维数组
ky     : Ky 坐标，一维数组
E      : 能量坐标，一维数组
time   : 时间 / delay 坐标，一维数组
```

说明：

- 程序会自动搜索 `npz` 中的主数据数组，优先识别 4D，再识别 3D
- 若坐标缺失，程序会按像素索引自动生成默认坐标
- 读入后内部会统一整理为 `(Kx, Ky, E, T)` 的分析格式
- 对于静态数据，程序会自动补一条长度为 1 的时间轴

### `.mat` 导入

导入 `.mat` 时，程序会先转换为 `.npz`。当前转换逻辑会优先识别以下常见字段别名：

- 主数据：`sample` / `binned` / `data`
- Kx 坐标：`kx` / `X`
- Ky 坐标：`ky` / `Y`
- 能量坐标：`E` / `energy`
- 时间坐标：`time` / `delay` / `t` / `T`

转换逻辑位于 [data_trans.py](data_trans.py)，如需要添加字段可自行更改。

## 快捷键

- ctrl切换到上一页，shift切换到下一页
- 数字键切换到指定页
- 光标在右侧控件页时快捷键对控件页生效，光标在左侧视图页时快捷键对视图页生效
- 所有下拉框（包含只读下拉框和读写下拉框）内的值可由鼠标滚轮快捷更改


## 控件功能说明

### 1. 图像控制页

推荐先在“图像控制”页完成基础数据准备：

- 点击“加载”导入 `npz` 或 `mat`
- 使用时间轴滑块切换时间帧；对于静态数据，时间轴仅保留一个位置
- 通过 `X / Y / Z` 上下限输入框设置切片立方体范围
- 需要二维切片时，将任一轴的上下限设为同一个位置，点击"截取"
- 勾选“显示坐标”显示物理坐标轴
- 勾选“切片交互”启用交互切片框
- "图像翻转"开关用于翻转左侧视图，既可用于3d图像也可用于2d图像及1d曲线
- 通过“截图”保存当前视图，通过“返回”恢复完整数据范围

### 2. 渲染控制页

在“渲染控制”页可以进一步调整显示效果：

- 切换色带
- 调整黑场 / 灰场 / 白场
- 切换强度映射方式
- 组合多个去噪步骤（当前仅Savitzky-Golay 和小波去噪参数可调）

### 3. 处理分析页

在“处理分析”页可以生成新的结果页：

- 对 4D 数据做时间积分
- 对 `X / Y / E` 任一轴做区间积分
- 计算切片内强度积分
- 计算当前时间帧的等能面内所有点的强度积分（能级态密度）
- 单条edc曲线及edc瀑布图（多条edc曲线自动错位）
- 用二阶导锐化左侧2d图像
- 使用“左侧视图保存”保存当前结果图
- 使用“视图数据保存”导出当前结果页对应的数据

## 导出结果说明

不同结果页会导出不同结构的数据：

- 主页面切片立方体：导出当前切片范围内的 `sample`（sample代表多维数据体） 以及对应的 `kx` / `ky` / `E` / `time`
- 时间积分结果：导出积分后的 `sample` 以及 `kx` / `ky` / `E`
- 坐标轴积分结果：导出积分后的 `sample`，以及剩余坐标轴与 `time`
- 切片内强度积分：导出 `time` 与积分强度 `intensity`
- 能级态密度：导出当前时刻、`E` 与谱强度 `intensity`

导出时可保存为 `MATLAB Files (*.mat)` 或 `NumPy Files (*.npz)`。

## ‘处理分析’页面中‘其他积分’内所含各种功能的说明

### 1. 切片内强度积分

将用户设置的切片内所有点按强度积分起来，横坐标为时间，纵坐标为切片内的总强度，点击‘应用’按钮后左侧会新增相应的视图页面

- 用户必须先在‘图像控制’页面进行切片立方体设置再点击‘应用’，否则会弹出报错框
- 不可用于没有时间轴的静态谱，否则会弹出报错框
- 可以被‘图像控制’里的‘坐标反转’开关调控
- 可以被‘渲染控制’里的‘去噪设置’调控，注意

### 2. 能级态密度

将等能面内所有点按强度积分起来，横坐标为能量，纵坐标为等能面内总强度

- 可以被‘图像控制’里的时间轴滑条调控
- 可以被‘图像控制’里的‘坐标反转’开关调控
- 可以被‘渲染控制’里的‘去噪设置’调控

### 3. EDC瀑布图

将‘对坐标轴积分’（仅限对kx与ky轴）后的2d图像内按照步长选取一组动量值，画出这些动量值上的能量分布曲线（横轴为强度，纵轴为能量），并将他们自动错位）

- 点击应用时，左侧视图页面必须为‘对坐标轴积分’应用后生成的2d视图页面，否则会弹出报错框
- 用户可以再左侧‘去噪参数设置’页面内自设动量值的选取步长
- ‘对坐标轴积分’里的积分上下限会影响最终的曲线形状
- 可以被‘图像控制’里的时间轴滑条调控
- 可以被‘渲染控制’里的‘去噪设置’调控

___开启"切片交互"后，可用鼠标左键在2d视图内框选矩形区域，点击"截取"按钮可以生成新的裁剪后的结果页，可用于限制edc瀑布图k值的取值范围___
### 4. 二阶导

强度对能量二阶求导后的2d图像，可用于锐化能带

- 点击应用时，左侧视图页面必须为‘对坐标轴积分’应用后生成的2d视图页面，否则会弹出报错框
- 可以被‘图像控制’里的时间轴滑条调控
- 可以被‘图像控制’里的‘坐标反转’开关调控
- 可以被‘渲染控制’里的‘色带选择’调控
- 可以被‘渲染控制’里的‘去噪设置’调控

### 5. 单条EDC曲线

开启"切片交互"后，用鼠标左键在2d视图内框选矩形区域，截取后生成单条edc曲线

- 点击应用时，左侧视图页面必须为‘对z坐标轴积分’应用后，再进行截取后生成的结果页，否则会弹出报错框
- 可以被‘图像控制’里的时间轴滑条调控
- 可以被‘图像控制’里的‘坐标反转’开关调控
- 可以被‘渲染控制’里的‘去噪设置’调控

## 已知限制与说明

- 静态 3D 数据无法真实展示时间积分与切片内强度积分的动态能力
- README 中提到的样例路径仅用于说明截图来源，不要求用户按该绝对路径组织数据
- 目前的去噪设置中仅“Savitzky-Golay滤波”和“小波去噪”参数可调，其他去噪方式的可调参数或许会在后续更新中加入

## 建议与反馈
- 若有其他改进建议或需添加功能或者bug反馈欢迎联系作者
- qq:1277514490
- 邮箱:jiaxinyu25@mails.ucas.ac.cn
- 打赏:![money](docs/images/money.png)
